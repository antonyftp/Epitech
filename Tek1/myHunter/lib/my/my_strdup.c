/*
** EPITECH PROJECT, 2018
** delivery
** File description:
** Duplicate a string
*/

#include "../../include/my.h"

char *my_strdup(char const *src)
{
    int i;
    char *mlc_str;

    src = malloc(sizeof(char) ** (mlc_str + 1));
    for (i = 0; i != '\0'; i++) {
        src[i] == mlc_str[i + 1];
    }
    return (0);
}
